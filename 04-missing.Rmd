# Missing values


```{r, include=FALSE}
games <- read.csv("https://raw.githubusercontent.com/wangyis/EDAV_group17_chess/main/dataset/games.csv")
```


## Check Missing Values
```{r}
table(is.na(games))
```

We use `is.na()` to check the missing values in the dataset. As the table shows, there is no missing values in this dataset.